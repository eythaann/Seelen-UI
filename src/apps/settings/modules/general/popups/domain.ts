export interface PopupState {
  enable: boolean;
  x: number | null;
  y: number | null;
  height: number;
  width: number;
  textColor: string;
  background: string;
  borderColor: string;
  borderWidth: number;
};