id: "@default/user-folder"
icon: FaRegCircleUser
target: "@seelen/fancy-toolbar"
plugin:
  type: user
  withUserFolder: true
  template: |-
    concat(
      imgFromPath(user.profilePicturePath),
      " ",
      user.name
    )
  style:
    flexShrink: 0