- name: Base
  identifier:
    id: (?i)((install)|(setup)|(complete)|(menu)|(notification))
    kind: Title
    matching_strategy: Regex
  options:
    - unmanage
- name: Seelen
  identifier:
    id: seelen-ui.exe
    kind: exe
    matching_strategy: equals
  options:
    - unmanage